# Cloudflare Pages Deploy

- Root directory: `.`
- Build command: `(none)`
- Output directory: `site`

Notes:

- This deploy is a landing page for the native app.
- The real EcoTide experience remains on device/simulator via SwiftUI.
